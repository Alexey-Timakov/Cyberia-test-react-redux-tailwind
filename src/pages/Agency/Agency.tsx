import { Form, PageTitle, VerticalDivider } from "@/components";

export const Agency = () => {
  return (
    <div>
      <PageTitle />

      <VerticalDivider verticalGap={6.5} />

      <Form />
    </div>
  )
};